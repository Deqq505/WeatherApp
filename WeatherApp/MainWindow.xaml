<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Weather App" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#FF1C1C1E" Foreground="White"
        WindowStyle="None" AllowsTransparency="True">
    <Grid>
       
        <Border Background="#FF007ACC" Height="30" VerticalAlignment="Top" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🗕" FontSize="14" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="MinimizeButton_Click"/>
                <Button Content="🗖" FontSize="14" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="MaximizeButton_Click"/>
                <Button Content="✕" FontSize="14" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,30,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                
                <Border Grid.Row="0" Background="#FF007ACC" Padding="15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Weather App" FontSize="28" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                </Border>

                
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20,20,20,10">
                    <TextBox x:Name="CityTextBox" Width="400" Padding="10" FontSize="18" Background="#FF2D2D30" Foreground="White" BorderBrush="#FF007ACC" 
                             Text="Wpisz miasto" GotFocus="CityTextBox_GotFocus" LostFocus="CityTextBox_LostFocus"/>
                    <Button x:Name="GetWeatherButton" Content="Szukaj" Width="120" Margin="15,0,0,0" Padding="10" FontSize="18" Background="#FF007ACC" Foreground="White" BorderBrush="#FF005A9E" Click="GetWeatherButton_Click"/>
                </StackPanel>

                
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20,10,20,20">
                    
                    <Image x:Name="WeatherIcon" Width="120" Height="120" Margin="0,0,25,0"/>
                    
                    <StackPanel>
                        <TextBlock x:Name="CityText" FontSize="26" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10" Foreground="#FF007ACC"/>
                        <TextBlock x:Name="TemperatureText" FontSize="22" Margin="0,5,0,0" Foreground="White"/>
                        <TextBlock x:Name="WeatherDescriptionText" FontSize="18" Margin="0,5,0,0" Foreground="White"/>
                        <TextBlock x:Name="HumidityText" FontSize="18" Margin="0,5,0,0" Foreground="White"/>
                    </StackPanel>
                </StackPanel>

                
                <Grid Grid.Row="3" Margin="20,10,20,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    
                    <StackPanel Grid.Column="0" Margin="10">
                        <TextBlock Text="Ciśnienie:" FontSize="18" Foreground="#FF007ACC"/>
                        <TextBlock x:Name="PressureText" FontSize="16" Margin="0,5,0,0" Foreground="White"/>
                    </StackPanel>

                    
                    <StackPanel Grid.Column="1" Margin="10">
                        <TextBlock Text="Wschód słońca:" FontSize="18" Foreground="#FF007ACC"/>
                        <TextBlock x:Name="SunriseText" FontSize="16" Margin="0,5,0,0" Foreground="White"/>
                        <TextBlock Text="Zachód słońca:" FontSize="18" Margin="0,10,0,0" Foreground="#FF007ACC"/>
                        <TextBlock x:Name="SunsetText" FontSize="16" Margin="0,5,0,0" Foreground="White"/>
                    </StackPanel>
                </Grid>

                
                <lvc:CartesianChart Grid.Row="4" x:Name="TemperatureChart" Margin="20,10,20,20" LegendLocation="Top" Height="300">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries Title="Temperatura (°C)" Values="{Binding Temperatures}" PointGeometrySize="10" Stroke="#FF007ACC" Fill="Transparent"/>
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Data" Labels="{Binding Dates}" LabelsRotation="45" Foreground="White"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Temperatura (°C)" Foreground="White"/>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.Background>
                        <SolidColorBrush Color="#FF2D2D30"/>
                    </lvc:CartesianChart.Background>
                </lvc:CartesianChart>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>